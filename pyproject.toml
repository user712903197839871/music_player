[project]
name = "music-utility"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "user712903197839871", email = "sinchetrup@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "levenshtein>=0.27.5",
    "pytest>=9.1.1",
]

[project.scripts]
music-utility = "music_utility:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
